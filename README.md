# Readme

The main website for the Bitcoin Dev Kit Foundation.

## Developing Locally

To develop locally ensure you have `uv` and `just` installed on your machine, and simply run `just serve` and open your browser to [http://localhost:8000/](http://localhost:8000/).
